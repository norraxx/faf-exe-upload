# faf-exe-upload
Frontend for uploading exe files

1. To build image use `./docker-build.sh`.
2. To run image use `./docker-run.sh`.
3. To upload image on docker hub use `docker login` + `./docker-push.sh` or use tag
